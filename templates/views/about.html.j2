{{> top}}

<p />
{{ title }} is a way to monitor active queries on MySQL instances from a
web interface. This makes it easier for non-DBAs to see what's happening
with queries in the database. If there are long-running queries, they'll
show up in this tool.

{{> bottom}}
